[project]
name = "dnd-character-sheet-manager"
version = "0.2.0"
requires-python = ">=3.13"

[tool.uv.workspace]
members = ["backend", "bot"]

[tool.uv.sources]

[tool.pytest.ini_options]
pythonpath = ["backend", "bot"]
testpaths = ["backend/tests", "bot/tests"]